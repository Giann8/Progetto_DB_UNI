services:
  postgres:
    build:
      context: ./database
      dockerfile: dockerfile
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: Graziano
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
